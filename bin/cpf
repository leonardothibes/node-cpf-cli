#!/usr/bin/env node

'use strict';

const argv  = require('node-argv'),
      ctrl  = require('../lib/controller'),
      error = require('../lib/errorHandler');

try {
    const args = argv(process.argv.slice(2));
    ctrl.handle(args);
} catch (e) {
    error.print(e);
}
